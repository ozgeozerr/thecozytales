---
layout: base.njk
title: The Cozy Tales
---

<section class="hero container section">
  <h1>The Cozy Tales</h1>
  <p>Your warm corner for pixel art, indie games, crafts, and heartwarming stories.</p>
  <div class="hero-buttons">
    <a href="#featured" class="btn btn-primary">Explore Posts</a>
    <a href="/subscribe/" class="btn btn-secondary">Join Community</a>
  </div>
</section>

<section class="latest-news container section scroll-box">
  <h2>Latest News</h2>
  <ul>
    {% for post in collections.latestNews | slice(0, 4) %}
      <li>
        <a href="{{ post.url }}">{{ post.data.title }}</a>
        <time datetime="{{ post.date }}">{{ post.date | date("MMM d") }}</time>
      </li>
    {% endfor %}
  </ul>
  <p><a href="/latest-news/">See all news →</a></p>
</section>

<section id="featured" class="container section">
  <h2 class="section-title">Latest Posts</h2>
  <div class="carousel">
    {% for post in collections.latestAddedPosts %}
      {% set url = post.url %}
      {% set title = post.data.title %}
      {% set excerpt = post.data.excerpt %}
      {% set date = post.data.date %}
      {% set headerImage = post.data.headerImage %}
      {% include "post-card.njk" %}
    {% endfor %}
  </div>
</section>

<section class="container section">
  <h2 class="section-title">Featured Game Blogs</h2>
  <div class="grid">
    {% for post in collections.gameBlogs | slice(0,3) %}
      {% set url = post.url %}
      {% set title = post.data.title %}
      {% set excerpt = post.data.excerpt %}
      {% set date = post.data.date %}
      {% set headerImage = post.data.headerImage %}
      {% include "post-card.njk" %}
    {% endfor %}
  </div>
  <p class="text-center"><a href="/game-blogs/" class="btn btn-secondary">View all game blogs</a></p>
</section>

<section class="container section">
  <h2 class="section-title">Featured Crafts &amp; Arts</h2>
  <div class="grid">
    {% for post in collections.craftsArts | slice(0,3) %}
      {% set url = post.url %}
      {% set title = post.data.title %}
      {% set excerpt = post.data.excerpt %}
      {% set date = post.data.date %}
      {% set headerImage = post.data.headerImage %}
      {% include "post-card.njk" %}
    {% endfor %}
  </div>
  <p class="text-center"><a href="/crafts-arts/" class="btn btn-secondary">View all crafts & arts</a></p>
</section>

<section class="container section">
  <h2 class="section-title">Featured Tales &amp; Stories</h2>
  <div class="grid">
    {% for post in collections.tales | slice(0,3) %}
      {% set url = post.url %}
      {% set title = post.data.title %}
      {% set excerpt = post.data.excerpt %}
      {% set date = post.data.date %}
      {% set headerImage = post.data.headerImage %}
      {% include "post-card.njk" %}
    {% endfor %}
  </div>
  <p class="text-center"><a href="/tales/" class="btn btn-secondary">Read all tales</a></p>
</section>

<section class="newsletter container section pixel-border">
  <h2> Stay Cozy </h2>
  <p>Get the latest posts, exclusive content, and creative resources delivered to your inbox!</p>
  <form class="subscribe-form">
    <input type="email" placeholder="Your email address" required>
    <button type="submit">Subscribe</button>
  </form>
</section>