---
layout: base.njk
title: Latest News
permalink: /latest-news/
pagination:
  data: collections.latestNews
  size: 10
  alias: posts
class: "category-page"
---
<section class="category-header pixel-border">
  <h1 class="category-title">Latest News</h1>
  <p class="category-tagline">What's going on in The Cozy Tales?</p>
</section>
<section class="category-posts">
  <ul class="post-list">
    {% for postItem in posts %}
      <li>
        {% set url = postItem.url %}
        {% set title = postItem.data.title %}
        {% set excerpt = postItem.data.excerpt %}
        {% set date = postItem.data.date %}
        {% set headerImage = postItem.data.headerImage %}
        {% include "post-card.njk" %}
      </li>
    {% else %}
      <li class="no-posts-message">
        <div class="pixel-border">
          <h3>Level Up Coming Soon!</h3>
          <p>We're working on some cozy game reviews! Check back soon!</p>
        </div>
      </li>
    {% endfor %}
  </ul>
</section>